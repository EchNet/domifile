[project]
name = "domifile"
version = "0.1.0"
requires-python = ">=3.13"

dependencies = [
  "fastapi",
  "uvicorn",
  "psycopg[binary]",
  "pgvector",
  "google-api-python-client",
  "google-auth",
  "pdfminer.six",
  "python-docx",
  "openai"
]
